# 《空域撤离》原型 · 审计修复执行记录（2026-08-18 凌晨）

> 依据：`docs/audit-report-2026-08-17.md`（Boss 全批）· 修改对象：`prototype/game.js` · 未动 index.html

## 已执行修复（13 处代码变更）

### P0 严重 Bug
| # | 修复 | 位置 |
|---|------|------|
| C1 | `damagePlayer()` 末尾加 `player.iframe = Math.max(iframe, 0.6)`——受击无敌帧，贴身敌不再每帧结算 | damagePlayer |
| C2 | ① `collectNode()` 宝箱入包统一走 `pushToLoot`（8 格上限），被 #197 筛选过滤的稀有度改掉地面（E 键可捡回）；② `pushToLoot` 新增 `occupied` 参数（宝库房按 run.loot 合并判满）；③ `exitRift/dieInRift` 并包改走 `pushToLoot` 防超载；④ rift 标志不再被覆盖（保住阵亡 50% 保底） | collectNode / pushToLoot / updateRift / exitRift / dieInRift |

### P1 数值平衡
| # | 修复 | 现有 → 调整 |
|---|------|------------|
| B1 | Boss 血量公式 | `(620+时间×5)` → `(620 + clamp(nodes-need,0,6)×60 + min(时间,240)×2)`：进度为主 + 封顶时间压力，300s 搜刮流从 3.4× 降到 ≤1.8× |
| B2 | 转幕猎杀 | 全量回血 → 等比放大 `hp×1.3` 保留已造伤害；`HUNT_DMG 1.4→1.25` |
| B5 | 猎杀预警精英 & 金/秘箱护卫 | `spawnEnemy` 新增 `opts.elite` 统一走精英分支（3× 血 + 精英掉落 + 修饰词），删除后置 `elite=true` 手动 ×1.5/×3 |

### P2 体验优化 + 参数微调
| # | 修复 |
|---|------|
| B3 | 新增 `diffTier() = min(4, run.tier + floor(gameTime/90))` 统一掉落难度口径，敌人/宝箱三处调用点全部替换 |
| B4 | `pickRarityWeighted` 白装权重 `floor≥1→0` 改为 `max(0, 20-floor×10)`（木箱 10 权重，合成燃料恢复） |
| B6 | `critMult` 上限 3.0（calcDamage 内 clamp） |
| M1 | Boss 血条四 kind 全量名字映射（饕餮·吞噬熔炉 / 混沌·终焉虚空） |
| M2 | killBoss 两条 banner 合并为一条（含遗物名 + 撤离窗口提示） |
| M3 | split 死亡改 `spawnEnemy(..., {arche:'split'})` 直建，蜂群多刷/横幅副作用消除 |
| M6 | Escape 分支补 mergeOverlay——合成台可用 Esc 关闭 |

## 未执行（报告中判定可不动）
- M4 新手提示与 banner 重叠（视觉层，待浏览器实测再定）；M5 升级曲线（有 30 级兜底）；M7 背包超载显示（随 C2 消失）
- B7 竞技房统领：保持原手动 ×6 调参不走精英分支，风险为零，总量不变

## 验证结果
- `node --check` 语法通过（6665 行）
- Node 桩运行时校验（真实执行 game.js）：**15 项检查全 PASS，0 运行时错误**
  - 场景切换 / 转幕 / Boss 生成（hundun 589 HP）/ 宝箱全收集 / 背包恰好 8/8 不超载 / 筛选后无 white·green 直入且被滤件落地面 / M6 开合 / 键盘 M·L·B·E·Q·F·G / 六移动按钮触控 / 600 帧战斗压力段
- 临时桩文件已删除；STUB_HOOK 块（含新增 collectNearest/spawnBoss/boss/runPhase/enemies/nodesCount 钩子）保留至发版前统一移除
- 预览：`http://localhost:8768/prototype/`（no-cache 服务运行中）

## 建议浏览器实测点
1. 被小怪贴身撞一下——应掉一次血后有无敌帧呼吸，不再秒杀
2. 转幕（收集约 72% 箱子）时已打残的敌人不再瞬间回满
3. Boss 血量对慢速搜刮流是否手感合适（现约为原 40-55%）
4. 开宝箱 + L 筛选白绿 → 箱子出的白绿件应变成地面暗化掉落物
