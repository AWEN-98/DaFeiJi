# 横屏 + 竖屏双朝向支持 · 验收报告

## 一句话结论
之前游戏**只认横屏**——竖屏时直接弹全屏遮罩逼你转手机，根本没有竖屏布局。
现在改成了**横屏、竖屏都能直接玩**，系统自动识别方向并切换布局，不再强制旋转。

## 改了什么

| 项目 | 原来 | 现在 |
|---|---|---|
| 旋转强制 | 竖屏弹全屏"请旋转至横屏"封锁 | 取消封锁，横竖都能进 |
| 布局 | 只有一套横屏布局 | 自动识别方向，竖屏单独适配（控件缩小重排、HUD 收窄） |
| 火力 | 必须用手按住右侧"开火"键 | 竖屏**默认自动开火**（单手难一边走位一边按火力，自动开火是同类手游标配） |
| 竖屏提示 | 全屏拦截 | 仅在标题页顶部一条软提示，可一键"继续竖屏游玩"关闭 |

## 两种方向怎么玩
- **横屏（推荐，视野最宽）**：左手左半屏滑动控制移动，右手按右侧"开火/绝技/闪避/丹药"，左上角一排小按钮管暂停/翻相/合成/拾取/背包。这是原来的主方案，保持不变。
- **竖屏（单手友好）**：控件同样布局但整体缩小、更贴合窄长屏；**飞机自动开火**，你只用左手走位、右手随时点绝技/闪避即可。想手动控火力也行——右侧"开火"键按住仍能开火。
- **自动开火开关**：进游戏按暂停（左上 ⏸）→ 有"自动开火：开/关"按钮，随时切换。

## 验证（桩测试，零报错）
- **桌面桩** `stub_check.js`：主循环 + 弹层开关 + 横幅队列，连跑 **8/8 = 0 报错**（顺手修了一个偶发 flaky：随机升级弹层会冻结横幅计时）。
- **新增竖屏桩** `stub_mobile.js`：用 iPhone 竖屏环境跑真实代码，断言 ①方向标记为 portrait ②竖屏自动开火默认开启 ③九键触控 + 画面渲染无报错 ④暂停开关能关自动开火 —— **3/3 = 0 报错**。

## 部署状态
- 本机预览已刷新（含新代码）：`http://127.0.0.1:8765/`
- 对外公开试玩链接：CloudStudio 云端**磁盘已满（ENOSPC）**两次发布失败，纯云端问题，**非代码**。等云端恢复空间后重跑一次发布即可刷新公开链接（「设置 - 数据管理 - 我发布的应用」可管理）。

## 备注
- 竖屏自动开火默认开启是为手感考虑；若 Boss 试玩后觉得太轻松，可在暂停里关掉，或我改成默认关闭。
- 横屏仍是视野最优方案，竖屏更适合通勤单手快速一局。
