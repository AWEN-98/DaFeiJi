# 手机端 UI 专项重构 · 验收报告

> 目标：把 PC 原型改成「手机上能舒服玩」的版本——双手拇指够得着、关键信息不挡视线、刘海/圆角不挡按钮、多点触控不卡死。
> 状态：**代码重构已完成并通过自动化验收（桩测试稳定 0 报错）**。本机预览可用；对外公开链接因云端磁盘满暂时未刷新。

---

## 一、按键怎么摆（双手拇指黄金区）

把操作分成左右两簇，模拟「主机手柄」的手感——左手管走位+工具，右手管开火+技能，互不打架。

| 区域 | 按钮 | 位置（相对屏幕） | 大小 | 作用 |
|---|---|---|---|---|
| **右下·动作簇**（右手拇指） | 开火 `fire` | 右下角 | 96px（最大） | 主火力，常按 |
| | 绝技 `ult` | 开火左上方 | 66px | 大招（充能满才亮） |
| | 闪避 `dash` | 开火左方 | 60px | 位移保命 |
| | 消耗品 `cons` | 开火下方 | 60px | 嗑药 |
| **左上·工具网格**（左手拇指） | 暂停 `pause` | 左上角 | 52px | 暂停/菜单 |
| | 相位 `phase` | 暂停右边 | 52px | 翻转相位（赌注） |
| | 合成 `merge` | 暂停下方 | 52px | 打开合成台 |
| | 拾取 `pickup` | 合成右边 | 52px | 吃鸡式捡装备列表 |
| | 背包 `bp` | 合成下方 | 52px | 查看战利品 |
| **左下·虚拟摇杆** | 摇杆 `joy` | 左半屏任意点按下即出现 | 底座132/摇杆58 | 移动 |

**弧形排布**：右下四个按钮不是排成一行，而是绕着右下角呈弧形（绝技/闪避/消耗品斜着围住开火键），拇指小范围移动就能全够到，不用来回大幅滑动。

**修掉的致命 bug**：旧版「筛选键」和「合成键」坐标完全重叠、「拾取键」和「背包键」也重叠，等于两个按钮互相挡住、只有一个能按。新版改成清晰的两列工具网格，互不遮挡。

---

## 二、HUD 信息怎么收（不挡视线）

把满屏乱飞的信息收敛到边缘，战斗时眼睛只盯中间敌人和弹幕：

- **顶部状态条**：血条、收集进度、四幕徽章收到屏幕最上沿，半透明微质感底，不挡中间。
- **右上信息列**：相位卡（鎏金↔余烬）从「左上」挪到「右上」，避开左上那排工具按钮——之前它压在按钮上，既挡按钮又看不清。
- **右上状态面板**：击杀/战利品/搜刮点数，角落一行小字。
- **小地图**：右上偏下，菱形点显示灵脉。
- **横幅提示**移到中上方，且不和顶部血条、连击数叠在一起（旧版会糊成一团）。
- **背包折叠**：手机上不再画一整块背包面板（战利品数已在右上状态面板显示），详情走 🎒 浮层，省空间。

---

## 三、安全区怎么处理（刘海/圆角/底部横条）

手机有「挖孔屏、刘海、圆角、底部手势条」，按钮如果贴边会被吃掉。做法：

- 用一个**隐藏探针**读出系统给的 `env(safe-area-inset-*)`（上/右/下/左四个安全边距），转成像素值 `SA={上,右,下,左}`。
- 这个 `SA` 值**贯穿所有 HUD 绘制**：血条、状态面板、相位卡、小地图、底部提示……全部按 `SA` 往里缩，绝不被刘海或底部横条盖住。
- DOM 按钮同步用 `calc(像素 + 安全边距)` 偏移。
- 适配范围从 16:9 到 20:9 窄长屏都验证过。

---

## 四、触控手感与多指健壮性

- **三态反馈齐全**：每个虚拟键都有「按下态」（缩小+变亮）、「冷却/禁用态」（变灰）、「就绪态」（金色呼吸高亮）。拾取键附近有可捡物时会金色呼吸提示。
- **多指不卡死**：之前闪避/消耗品等按钮缺「手指抬起/取消」的复位，多指同时操作可能卡住。现已给全部 9 个按钮补上 `touchcancel` 复位。
- **列表打开不误触**：吃鸡式拾取列表展开时，加了一道「闸门」——底层不再响应开火/技能，避免一边看列表一边走火。

---

## 五、解决的具体痛点（清单）

| # | 痛点 | 修复 |
|---|---|---|
| 1 | 筛选键/合成键、拾取键/背包键坐标完全重叠，互相遮挡无法点击 | 重写为不重叠的两列工具网格 |
| 2 | DOM 按钮用了安全区，但 Canvas 里画的 HUD 全用固定像素，刘海照样挡 | 安全区探针 `SA` 贯穿所有 HUD 元素 |
| 3 | 相位卡压在左上工具按钮上，又挡按钮又看不清 | 相位卡移到右上信息列 |
| 4 | 满屏 HUD 信息杂乱、互相重叠 | 收敛到边缘+半透明、横幅防叠、背包移动端折叠 |
| 5 | 多指操作偶发卡死/丢帧 | 9 按钮补全 `touchcancel` 复位 |
| 6 | 拾取列表打开时底层仍响应开火 | `pickupOpen` 闸门拦截底层触控 |
| 7 | 拾取列表行太小、可能溢出屏幕 | 行高加大(46px)、背景压暗、安全区垂直居中 |
| 8 | 虚拟键无按下/冷却/就绪反馈 | 补齐 active / cooldown / glow 三态 |

---

## 六、自动化验收结果

- `node --check game.js`：语法 **通过**。
- `stub_check.js`（Node 内存桩真实跑游戏主循环 + 9 按钮触控 + 弹层开关 + banner 队列 + 600 帧高压）：**稳定 0 报错**。
  - 过程中发现并修复了一个**偶发 flaky 测试**：原测试用笼统的「是否有弹层打开」去判断「合成层是否关闭」，而随机模拟里玩家升级会弹出符文选择层，污染了确定性开关断言（5 跑 2 失败）。已加 `cleanState()` 强制清场 + 改为直接检查合成层显示态，现 6/6 跑全过。

---

## 七、试玩部署状态

- **本机预览**：已起静态服务器 `http://127.0.0.1:8765/`（HTTP 200，可直接用浏览器手机模拟器验证）。仅本机可用。
- **对外公开链接**：旧的 CloudStudio 链接已**下线**；新链接发布时云端返回**磁盘空间不足（ENOSPC）**，2 次重试均失败——这是云端基础设施问题，**非代码问题**。等云端空间恢复后，重跑一次发布即可刷新公开试玩链接（命令已就绪：`workbuddy_cloudstudio_deploy(deploy, playtest/)`）。
- 部署目录 `playtest/` 已用 `deploy_prep.js` 刷新为最新代码，且**已剥离测试专用 STUB 钩子**（发版干净）。

---

### 一句话总结
手机端重构按「双手拇指分区 + 边缘 HUD + 安全区贯穿 + 多指健壮」四原则落地，验收稳定 0 报错；本机可玩，公开链接等云端恢复空间后即可刷新。
